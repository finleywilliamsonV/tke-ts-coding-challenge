import { AutoFormatPipe } from './auto-format.pipe';

describe('AutoFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new AutoFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
